import React from 'react';import Link from 'next/link';import Image from 'next/image';interface LogoProps {    className?: string;    showText?: boolean;    size?: 'sm' | 'md' | 'lg' | 'xl';    hideText?: boolean;}export const Logo: React.FC<LogoProps> = ({ className = '', size = 'md', hideText = false }) => {    const sizeClasses = {        sm: 'text-lg',        md: 'text-2xl',        lg: 'text-3xl',        xl: 'text-6xl',    };    return (        <Link href="/" className={`group flex items-center transition-transform active:scale-95 ${className}`}>            {!hideText && (                <div className="flex items-center">                    <span className={`${sizeClasses[size]} font-black text-white tracking-[-0.05em]`}>Market</span>                    <span className={`${sizeClasses[size]} font-black text-[#FF6200] tracking-[-0.05em]`}>Bridge</span>                </div>            )}            {hideText && (                <div className="h-12 w-12 rounded-full bg-[#FF6200] blur-3xl opacity-50" />            )}        </Link>    );};